const express = require('express');
const router = express.Router();
const userController = require('../controller/');

//route about thing

/**
 *  router.post('/' , userController.function);
 */
module.exports = router;